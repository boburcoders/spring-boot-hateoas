spring:
  mail:
    host: 'sandbox.smtp.mailtrap.io'
    port: 2525
    username: 'username'
    password: 'password'
    properties:
      smtp:
        auth: true
        starttls:
          enable: true
  freemarker:
    template-loader-path: classpath:/templates/
